spring.application.name=library_managament

# MySQL database configuration
# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/library_managament?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Kartik$@&123

# JPA and hibernate settings
# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

debug=true
server.port=8181

server.servlet.logging.request-details=true
